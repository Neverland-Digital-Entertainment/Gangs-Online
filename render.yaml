services:
  - type: web
    name: gangs-online-server
    runtime: node
    buildCommand: npm install && npm run build --workspace=@gangs-online/shared && npm run build --workspace=@gangs-online/server
    startCommand: cd packages/server && npm start
    envVars:
      - key: NODE_VERSION
        value: 18.18.0
